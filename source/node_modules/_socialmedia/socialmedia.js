// STACK
var h       = require('virtual-hyperscript-hook')(require('virtual-dom/h'))
// CUSTOM
var styling = require('_styling')
var s
// @TODO: Social Media Feed:
// - boxen analog zu boxen
// => general electrics feed
module.exports = SocialmediaComponent

function SocialmediaComponent (state) {
  s = s ? s : styling(`
    /* add styling */
  `)
  return function Socialmedia (state) {
    return h('div', { className: '' }, [
      h('a', { href: '/#facebook'}, 'Facebook'),
      h('a', { href: '/#twitter'}, 'Twitter'),
      h('a', { href: '/#linkedin'}, 'LinkedIn'),
      h('a', { href: '/#xing'}, 'Xing'),
      h('a', { href: '/#instagram'}, 'Instagram')
    ])
  }
}
